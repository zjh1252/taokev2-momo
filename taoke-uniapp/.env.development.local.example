# 复制为 .env.development.local（已 gitignore）
# 场景：开发者工具 + 本机 Java 后端 + 手机真机调试（不是预览扫码 test 包）
#
# 1. ipconfig 查看本机 IPv4（如 192.168.1.100），手机与电脑同一 WiFi
# 2. 取消下行注释并改成你的 IP（API 与图片都走本机，避免 v2 自签证书）
# 3. bun run dev:mp-weixin，导入 dist/dev/mp-weixin
# 4. 详情 → 本地设置 → 勾选「不校验合法域名、web-view、TLS 版本以及 HTTPS 证书」
# 5. 点「真机调试」扫码（不要用「预览」——预览时手机无法访问 localhost/局域网）
# 6. Windows 防火墙放行 8080；后端需在本机已启动
#
# VITE_API_BASE_URL_NATIVE=http://192.168.1.100:8080
# VITE_ASSET_BASE_URL_NATIVE=http://192.168.1.100:8080
