# buildx 推送私有 HTTP 仓库时使用（与 deploy/build.sh 配套）
[registry."10.0.14.20:5000"]
  http = true
  insecure = true

# Docker Hub 加速（buildx docker-container 驱动不读 Docker Desktop 的 registry-mirrors）
[registry."docker.io"]
  mirrors = ["https://docker.m.daocloud.io"]
