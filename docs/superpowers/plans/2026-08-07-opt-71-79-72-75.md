# 待优化优先批次 71/79/72/75 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按顺序交付内训课报名（71）、老账号弱密码登录（79）、专家「信得过」角标与筛选一致（72）、内训价格三态（75）。

**Architecture:** 71 镜像公开课报名独立 lead 栈（无 plan）；79 为 `sys_users.old_user` + 登录去格式校验；72 仅改前端角标条件对齐后端 OR；75 修迁移约定 + 内训侧栏/列表三态渲染。

**Tech Stack:** Java 21 / Spring Boot 3.5 / JPA / Flyway V159–V160 / Next.js 16 / pnpm / bun / Apache POI / uv + data-trans

**Spec:** `docs/superpowers/specs/2026-08-07-opt-71-79-72-75-design.md`

## Global Constraints

- 注释中文；类注释 `@author Fangxinxin` + `@date yyyy-MM-dd HH:mm`
- Admin 禁止注入 Repository；经 course/user 模块 `api/` 接口
- 布尔列名禁止 `is_` 前缀（用 `old_user`）；Java `Boolean`
- frontend 仅 pnpm；admin-frontend 仅 bun；Python 仅 `uv run`
- 新增/修改 Flyway 后：`uv run python data-trans/scripts/_validate_flyway_migration.py --version <N>`
- 71 数据不得写入 `open_course_enrollments` / `demands` / `trainer_lead_messages`
- 登录接口一律不做密码长度校验；注册/改密/重置保留 6–32
- 提交 commit 仅在用户明确要求时执行（计划中的 Commit 步骤默认跳过，或等用户授权）

---

## File Map

### §71 Create
- `backend/taoke-app/src/main/resources/db/migration/V159__create_internal_course_enrollments.sql`
- `backend/taoke-course/.../entity/enrollment/InternalCourseEnrollment.java`
- `backend/taoke-course/.../repository/enrollment/InternalCourseEnrollmentRepository.java`
- `backend/taoke-course/.../dto/enrollment/SubmitInternalCourseEnrollmentRequest.java`
- `backend/taoke-course/.../dto/enrollment/UpdateInternalCourseEnrollmentRequest.java`
- `backend/taoke-course/.../dto/enrollment/InternalCourseEnrollmentVO.java`
- `backend/taoke-course/.../api/InternalCourseEnrollmentService.java`
- `backend/taoke-course/.../service/enrollment/InternalCourseEnrollmentServiceImpl.java`
- `backend/taoke-course/.../controller/enrollment/InternalCourseEnrollmentController.java`
- `backend/taoke-admin/.../controller/AdminInternalCourseEnrollmentController.java`
- `frontend/.../detail/CourseEnrollFormFields.tsx`（共享表单）
- `frontend/.../detail/InternalCourseEnrollDialog.tsx`
- `admin-frontend/src/features/internal-course-enrollments/**`（镜像 open-course-enrollments）
- `admin-frontend/src/app/dashboard/internal-course-enrollments/page.tsx`
- `admin-frontend/src/app/api/internal-course-enrollments/**`

### §71 Modify
- `frontend/.../detail/OpenCourseEnrollDialog.tsx` — 改用共享表单
- `frontend/.../detail/CourseSidebar.tsx` — 内训 CTA → 立即报名 + 打开内训弹窗
- `frontend/.../course/api/service.ts` — `submitInternalCourseEnrollment`
- `admin-frontend/src/config/nav-config.ts` — 菜单「内训课报名」
- `admin-frontend/src/hooks/use-breadcrumbs.tsx` — 面包屑

### §79
- Create: `V160__add_users_old_user.sql`
- Modify: `User.java`, `UserProfileResponse.java`, `UserServiceImpl.getProfile`, `UsernameLoginRequest.java`, `LoginForm.tsx`, 改密/重置页提示文案；data-trans 迁用户处写 `old_user=1`

### §72
- Modify: `TrainerCard.tsx`, `TrainerHero.tsx`, `PxbTrainerListItem.tsx` 角标条件

### §75
- Modify: `run_legacy_courses_migrate.py`；新增 fixup 脚本；`CourseSidebar.tsx` 内训价区；`InnerCourseCard.tsx` 价格行

---

### Task 1: Flyway V159 + Entity + Repository（71）

**Files:**
- Create: `backend/taoke-app/src/main/resources/db/migration/V159__create_internal_course_enrollments.sql`
- Create: `backend/taoke-course/src/main/java/com/taoke/course/entity/enrollment/InternalCourseEnrollment.java`
- Create: `backend/taoke-course/src/main/java/com/taoke/course/repository/enrollment/InternalCourseEnrollmentRepository.java`

**Interfaces:**
- Produces: 表 `internal_course_enrollments`；实体字段无 `planId`；Repository 继承 `JpaRepository` + `JpaSpecificationExecutor`

- [ ] **Step 1: 写 Flyway（对照 V158，去掉 plan 列）**

```sql
-- ============================================================
-- 内训课报名线索表（与 open_course_enrollments / demands 隔离）
-- ============================================================

CREATE TABLE internal_course_enrollments (
    id               INT           NOT NULL AUTO_INCREMENT COMMENT '主键',
    user_id          INT           NULL                    COMMENT '提交人用户 ID（登录可选）',
    course_id        INT           NOT NULL                COMMENT '内训课 ID',
    real_name        VARCHAR(50)   NOT NULL                COMMENT '真实姓名',
    company_name     VARCHAR(200)  NOT NULL                COMMENT '公司名称',
    email            VARCHAR(100)  NOT NULL                COMMENT '电子邮件',
    company_phone    VARCHAR(30)   NULL                    COMMENT '公司电话',
    mobile           VARCHAR(20)   NULL                    COMMENT '手机号码',
    course_title     VARCHAR(500)  NOT NULL DEFAULT ''     COMMENT '提交时冗余课程名称',
    status           TINYINT(2)    NOT NULL DEFAULT 0      COMMENT '处理状态：0=待处理 1=已联系 2=已无效',
    admin_remark     TEXT          NULL                    COMMENT '运营备注',
    created_at       DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '报名提交时间',
    updated_at       DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    INDEX idx_ice_created_at (created_at),
    INDEX idx_ice_course_id (course_id),
    INDEX idx_ice_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='内训课报名线索';
```

- [ ] **Step 2: 校验 Flyway**

Run: `uv run python data-trans/scripts/_validate_flyway_migration.py --version 159`  
Expected: 通过（无 checksum/语法报错）

- [ ] **Step 3: Entity + Repository**

复制 `OpenCourseEnrollment` / `OpenCourseEnrollmentRepository`，改名；删 `planId`、`planStartTime`、`planEndTime`；`@Table(name = "internal_course_enrollments")`；`@DynamicInsert` / `@DynamicUpdate`；关联一律不配（无 FK）。

Repository:

```java
public interface InternalCourseEnrollmentRepository
        extends JpaRepository<InternalCourseEnrollment, Integer>,
                JpaSpecificationExecutor<InternalCourseEnrollment> {
}
```

---

### Task 2: Course 域提交与管理 API（71）

**Files:**
- Create DTOs / api / ServiceImpl / C Controller（路径见 File Map）
- Mirror: `OpenCourseEnrollmentService*` 全套

**Interfaces:**
- Consumes: `InternalCourseEnrollmentRepository`, `CourseRepository`
- Produces:
  - `Integer submit(Integer userId, SubmitInternalCourseEnrollmentRequest req)`
  - adminSearch / adminGetDetail / adminUpdate / adminListForExport（签名对齐 Open 版，无 plan 字段）
  - `POST /internal-course-enrollments` `@Public`

- [ ] **Step 1: Submit DTO（无 planId）**

```java
@Data
public class SubmitInternalCourseEnrollmentRequest {
    @NotBlank(message = "请填写真实姓名")
    private String realName;
    @NotBlank(message = "请填写公司名称")
    private String companyName;
    @NotBlank(message = "请填写电子邮件")
    @Email(message = "电子邮件格式不正确")
    private String email;
    private String companyPhone;
    private String mobile;
    @NotNull(message = "课程 ID 不能为空")
    private Integer courseId;
}
```

`UpdateInternalCourseEnrollmentRequest` / `InternalCourseEnrollmentVO`：从 Open 版复制，去掉 `planId`、`planStartTime`、`planEndTime`。

- [ ] **Step 2: ServiceImpl.submit 关键差异**

在 Open 版逻辑基础上：

1. 电话二选一校验保留  
2. `courseRepository.findById`；若不存在 → NOT_FOUND  
3. **必须** `course.getType() == CourseType.INTERNAL`，否则 PARAM_INVALID「仅支持内训课报名」  
4. **不要**查 `CoursePlan`  
5. 写 entity：`courseTitle` 冗余；`status=0`；`userId` 可为 null  

`toVo` 中课程已删标签：`DELETED_COURSE_LABEL = "课程已删除"`（与 Open 一致）。

- [ ] **Step 3: C 端 Controller**

```java
@RestController
@RequiredArgsConstructor
@Tag(name = "内训课报名")
public class InternalCourseEnrollmentController {
    private final InternalCourseEnrollmentService enrollmentService;

    @Public
    @PostMapping("/internal-course-enrollments")
    @Operation(summary = "提交内训课报名（登录可选）")
    public ApiResponse<Integer> submit(@Valid @RequestBody SubmitInternalCourseEnrollmentRequest request) {
        Integer userId = SecurityUtils.getCurrentUserIdOrNull(); // 与公开课一致的取法
        return ApiResponse.ok(enrollmentService.submit(userId, request));
    }
}
```

（若项目用 `SecurityUtils.getCurrentUserId()` 可空写法，以 `OpenCourseEnrollmentController` 为准一字不差对齐。）

---

### Task 3: Admin 薄编排 + Excel（71）

**Files:**
- Create: `backend/taoke-admin/src/main/java/com/taoke/admin/controller/AdminInternalCourseEnrollmentController.java`

**Interfaces:**
- Consumes: `InternalCourseEnrollmentService`（api 包）
- Produces:
  - `GET /admin/internal-course-enrollments`
  - `GET /admin/internal-course-enrollments/{id}`
  - `PUT /admin/internal-course-enrollments/{id}`
  - `GET /admin/internal-course-enrollments/export`

- [ ] **Step 1: 复制 `AdminOpenCourseEnrollmentController`**

全局替换命名；导出文件名 `内训课报名.xlsx`；表头去掉期次相关列；列顺序对齐 spec：ID｜提交时间｜真实姓名｜公司名称｜电子邮件｜公司电话｜手机号码｜关联内训课课程｜处理状态｜运营备注（导出可含备注）。

- [ ] **Step 2: `@RequireRole(SUPER_ADMIN)`** 与公开课报名一致，禁止注入 Repository。

---

### Task 4: C 端共享表单 + 内训弹窗 + Sidebar CTA（71）

**Files:**
- Create: `frontend/src/features/course/components/detail/CourseEnrollFormFields.tsx`
- Create: `frontend/src/features/course/components/detail/InternalCourseEnrollDialog.tsx`
- Modify: `OpenCourseEnrollDialog.tsx`
- Modify: `CourseSidebar.tsx`
- Modify: `frontend/src/features/course/api/service.ts`

**Interfaces:**
- Produces: `submitInternalCourseEnrollment(body)` → `POST /internal-course-enrollments`
- `CourseEnrollFormFields` props: `form`, `update`, `disabled?`

- [ ] **Step 1: 抽出共享字段 UI**

从 `OpenCourseEnrollDialog` 抽出姓名/公司/邮箱/公司电话/手机 + 底部「提醒:电话或手机可选填一个」。校验规则 `RULES` 与电话二选一逻辑两边共用（可放同文件或 `enroll-form.ts`）。

- [ ] **Step 2: `InternalCourseEnrollDialog`**

对齐 Open 弹窗标题/成功 toast；`submitInternalCourseEnrollment({ ..., courseId })`（无 planId）。

- [ ] **Step 3: `service.ts`**

```ts
export async function submitInternalCourseEnrollment(body: {
  realName: string;
  companyName: string;
  email: string;
  companyPhone?: string;
  mobile?: string;
  courseId: number;
}) {
  // POST /internal-course-enrollments — 复制 submitOpenCourseEnrollment 实现，改 path
}
```

- [ ] **Step 4: `CourseSidebar` 内训 CTA**

将约 L265–274：

- 文案：`联系客服购买` → `立即报名`
- `onClick`：打开 `InternalCourseEnrollDialog`（本地 `enrollOpen` state），**不要** `setConsultOpen(true)`
- className / 红色主按钮样式保持不变
- 在侧栏 JSX 挂载 `<InternalCourseEnrollDialog open={enrollOpen} onOpenChange={setEnrollOpen} courseId={course.id} />`

---

### Task 5: Admin 前端「内训课报名」（71）

**Files:**
- 复制 `admin-frontend/src/features/open-course-enrollments/` → `internal-course-enrollments/`
- 复制 `app/dashboard/open-course-enrollments/page.tsx` → `internal-course-enrollments/page.tsx`
- 复制 `app/api/open-course-enrollments/**` → `internal-course-enrollments/**`
- Modify: `nav-config.ts`, `use-breadcrumbs.tsx`

- [ ] **Step 1: 复制 feature + 全局替换**

`OpenCourse` → `InternalCourse`；`open-course-enrollments` → `internal-course-enrollments`；中文「公开课报名」→「内训课报名」；去掉 plan 列/类型字段。

- [ ] **Step 2: nav-config**

在「平台运营管理」分组、`公开课报名` 旁新增：

```ts
{
  title: '内训课报名',
  url: '/dashboard/internal-course-enrollments',
  icon: 'forms', // 与公开课报名同 icon 即可
  isActive: false,
  items: []
}
```

- [ ] **Step 3: breadcrumbs** 增加 `internal-course-enrollments: '内训课报名'` 与路由 crumbs。

- [ ] **Step 4: 手工冒烟清单（71）**

- [ ] 内训详情 CTA 为「立即报名」，样式不变  
- [ ] 弹窗校验：空姓名/坏邮箱/双电话空均拦截  
- [ ] 提交后后台列表有记录  
- [ ] 筛选/详情/改状态备注/导出可用  
- [ ] 公开课报名列表无该数据  

---

### Task 6: Flyway V160 + User.oldUser（79）

**Files:**
- Create: `backend/taoke-app/src/main/resources/db/migration/V160__add_users_old_user.sql`
- Modify: `backend/taoke-user/.../entity/User.java`
- Modify: `UserProfileResponse.java` + `UserServiceImpl.getProfile`

- [ ] **Step 1: Flyway**

```sql
-- 老站迁移账号标记（布尔列禁止 is_ 前缀）
ALTER TABLE sys_users
    ADD COLUMN old_user TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否老站迁移账号' AFTER user_source;

UPDATE sys_users SET old_user = 1 WHERE uc_uid IS NOT NULL;
```

（若 `user_source` 列位置不同，`AFTER` 可改为表尾或对照 V80 实际列序。）

- [ ] **Step 2: 校验** `uv run python data-trans/scripts/_validate_flyway_migration.py --version 160`

- [ ] **Step 3: Entity**

```java
@Column(name = "old_user", nullable = false, columnDefinition = "tinyint")
private Boolean oldUser = false;
```

`UserProfileResponse` 增加 `Boolean oldUser`；`getProfile` 映射该字段。

- [ ] **Step 4: data-trans** 用户迁入写 `old_user=1`（定位现有 insert users 脚本，补列；若脚本未列全字段则补默认）。

---

### Task 7: 登录去格式校验 + 改密提示（79）

**Files:**
- Modify: `UsernameLoginRequest.java`
- Modify: `frontend/src/features/auth/components/LoginForm.tsx`
- Modify: 个人中心改密页 / `ForgotPasswordForm.tsx`（提示文案）

- [ ] **Step 1: 后端登录 DTO**

`UsernameLoginRequest.password`：**删除** `@Size(min=6, max=32)`，仅保留 `@NotBlank`。

- [ ] **Step 2: LoginForm**

去掉「密码长度 &lt; 6 禁止提交」的前端拦截（保留空密码提示即可）。注册页 `RegisterForm` **不要改**。

- [ ] **Step 3: 改密/重置提示**

文案：「您是历史迁移账号，原有密码可正常登录；修改密码时，请设置6‑32位字符的新密码。」

- 登录态改密页：`user.oldUser === true`（或 `oldUser === 1`）时展示  
- 忘记密码页：可对所有人展示一句较短兼容说明，或登录后进改密再展示（优先改密页按 `oldUser`）

- [ ] **Step 4: 确认无「登录强制改密」弹窗逻辑**（搜索 force change password；若有则禁用对 oldUser）。

- [ ] **Step 5: 冒烟（79）**

- [ ] 弱密码老账号可登录  
- [ ] 新注册仍 6–32  
- [ ] 改密/重置仍 6–32 + 提示  
- [ ] 无强制改密弹窗  

---

### Task 8: 专家角标对齐优质口径（72）

**Files:**
- Modify: `frontend/src/features/trainer/components/list/TrainerCard.tsx`
- Modify: `frontend/src/features/trainer/components/detail/TrainerHero.tsx`
- Modify: `frontend/src/features/trainer/components/pxb/PxbTrainerListItem.tsx`

**Interfaces:**
- 展示条件：`isTrusted === 1 || isSigned === 1`
- 后端 `TrainerServiceImpl` OR 过滤 **保持不动**

- [ ] **Step 1: 统一角标条件**

三处均改为：

```tsx
{(trainer.isTrusted === 1 || trainer.isSigned === 1) && (
  // 原徽章 JSX 不变
)}
```

确认列表 VO / 类型含 `isSigned`（`types.ts` 已有则不用改）。

- [ ] **Step 2: 核对筛选持久化**

在 `TrainerListSection`：翻页、改 sort、勾选 `trustedOnly` 时 `isTrusted: 1` 仍传入 API；重置清空 `trustedOnly`。若发现 bug 则修；无 bug 不改。

- [ ] **Step 3: 冒烟（72）**

- [ ] 勾选信得过，首页与尾页均有「信」标  
- [ ] 排序切换后仍过滤  
- [ ] 重置恢复全量  

---

### Task 9: 内训价格迁移三态（75）

**Files:**
- Modify: `data-trans/scripts/run_legacy_courses_migrate.py`（约 L299–334）
- Create: `data-trans/scripts/fixup_internal_course_price_pending.py`（存量回填）

- [ ] **Step 1: 改 build_course_row 价格逻辑**

```python
legacy_price = money_or_none(row.get("min_special_price")) or money_or_none(row.get("min_price"))

if course_type.startswith("OPEN"):
    price = legacy_price if legacy_price is not None else Decimal("0.00")
    original_price = money_or_none(row.get("min_price"), zero_is_none=False) or price
    is_free = 1 if price <= 0 else 0
else:
    # 内训：有金额保留；无金额 → 待定（price=0, is_free=0）；禁止默认 is_free=1
    if legacy_price is not None and legacy_price > 0:
        price = legacy_price
        original_price = money_or_none(row.get("min_price"), zero_is_none=False) or price
        is_free = 0
    else:
        price = Decimal("0.00")
        original_price = Decimal("0.00")
        is_free = 0
```

（若老库存在明确「免费」标记字段，仅在该标记为真时 `is_free=1`；实现前在 `tk_courseinfo`/`tk_coursedata` 搜 free/免费相关列，有则接上，无则保持上式。）

- [ ] **Step 2: 存量 fixup 脚本**

`uv run python data-trans/scripts/fixup_internal_course_price_pending.py`：

- 读老库重算 INTERNAL 的 `price` / `is_free`  
- `UPDATE courses SET ... WHERE type='INTERNAL' AND id=?`  
- 支持 dry-run  

- [ ] **Step 3: 在测试库 dry-run → 实跑**（按 `docs/guides/data-trans-migration.md` 惯例）

---

### Task 10: 内训详情/列表价格 UI（75）

**Files:**
- Modify: `CourseSidebar.tsx`
- Modify: `InnerCourseCard.tsx`
- `CourseHero.tsx` 免费标保持 `isFree === 1`（待定不再误标，依赖 Task 9 数据）

- [ ] **Step 1: Sidebar 内训价区**

在 sticky 卡片顶部（按钮之前）增加：

```tsx
{isInternal && (
  <div className="text-center pb-4 border-b border-slate-100">
    <p className="text-sm text-slate-500 mb-1">培训参考价</p>
    <p className="text-3xl font-bold text-red-500">
      {course.isFree === 1
        ? '免费'
        : course.price > 0
          ? `¥${course.price.toLocaleString()}`
          : '待定'}
    </p>
  </div>
)}
```

样式对齐公开课价区（L218–228）；有金额时可同样展示划线原价（若 `originalPrice > price`）。

- [ ] **Step 2: InnerCourseCard 价格行**

在信息区增加一行，例如：

```tsx
<div className="flex text-sm text-slate-600">
  <span className={FIELD_LABEL_CLASS}>参考价：</span>
  <span>
    {course.isFree === 1 ? '免费' : course.price > 0 ? `¥${course.price}` : '待定'}
  </span>
</div>
```

确认 `CourseListItem` 含 `price` / `isFree`（已有则直接用）。

- [ ] **Step 3: 冒烟（75）**

- [ ] 待定课：无「免费」标，侧栏/列表为「待定」  
- [ ] 免费课：有免费标 + 「免费」  
- [ ] 有金额：¥ 样式正常  
- [ ] 公开课价逻辑不变  

---

### Task 11: 收尾自检

- [ ] Flyway 159、160 均已 `_validate_flyway_migration`  
- [ ] Spec 四节验收清单全部勾完  
- [ ] 未改 UniApp；未混表  
- [ ] （用户授权后）将本批次工作记入 `docs/process/works.md`

---

## Spec Coverage Self-Review

| Spec 项 | Task |
|---------|------|
| 71 独立表/API/菜单/弹窗/导出 | 1–5 |
| 71 登录可选、电话二选一、课程已删除 | 2–4 |
| 79 old_user + 回填 uc_uid | 6 |
| 79 登录无格式校验、改密提示、无强制改密 | 7 |
| 72 角标 OR、后端保持 OR | 8 |
| 75 三态约定 + 迁移 + 侧栏/列表 | 9–10 |
| 73/76/77/78 | 明确不在本计划 |

## Placeholder Scan

无 TBD；迁移「免费」标记若老库无列则按待定处理（已写明）。
