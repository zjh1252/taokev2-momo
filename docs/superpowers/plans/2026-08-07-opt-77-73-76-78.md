# 待优化后续批次 77/73/76/78 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按顺序交付首页城市频道静态清理（77）、专家留言后台可运营（73）、公开课已选筛选+URL+PXB（76）、内训课编号对齐老站（78）。

**Architecture:** 77 删 UI 占位；73 用 Flyway 权限种子 + `@RequirePermission` 替换类级超管限制并补导航；76 扩展公开课 URL 解析/写入与 chips，PXB 增加已选汇总行；78 内训 `displayCourseNo` 固定为 `courses.id`，Hero 改读该字段。

**Tech Stack:** Java 21 / Spring Boot 3.5 / Flyway V161 / Next.js 16 / pnpm / bun

**Spec:** `docs/superpowers/specs/2026-08-07-opt-77-73-76-78-design.md`

## Global Constraints

- 注释中文；类注释 `@author Fangxinxin` + `@date yyyy-MM-dd HH:mm`
- Admin 禁止注入 Repository；留言编排保持现有 `AdminTrainerMessageService` / `DemandService`
- Flyway 新脚本 **V161**；改后：`uv run python data-trans/scripts/_validate_flyway_migration.py --version 161`
- frontend 仅 pnpm；admin-frontend 仅 bun
- 本仓库 C 端暂不强制单测：验证以页面/接口自检为准
- 提交 commit 仅在用户明确要求时执行（计划中的 Commit 步骤默认跳过）

---

## File Map

### §77
- Modify: `frontend/src/features/city/components/CityChannelCard.tsx`

### §73
- Create: `backend/taoke-app/src/main/resources/db/migration/V161__trainer_message_manage_permission.sql`
- Modify: `backend/taoke-admin/.../AdminTrainerMessageController.java`
- Modify: `admin-frontend/src/config/nav-config.ts`
- Modify: `admin-frontend/src/hooks/use-breadcrumbs.tsx`

### §76
- Modify: `frontend/src/features/course/components/open/open-course-list-url.ts`
- Modify: `frontend/src/features/course/components/open/OpenCourseListSection.tsx`
- Modify: `frontend/src/app/[locale]/(public)/opencourses/page.tsx`
- Modify: `frontend/src/app/[locale]/(public)/city/[city]/opencourse/page.tsx`（若传 initial 筛选）
- Modify: `frontend/src/features/course/components/open/pxb/PxbCourseListSection.tsx`

### §78
- Modify: `backend/taoke-course/.../service/CourseServiceImpl.java`（`assembleDetail`）
- Modify: `frontend/src/features/course/components/detail/CourseHero.tsx`

---

### Task 1: 清理首页城市频道静态占位（77）

**Files:**
- Modify: `frontend/src/features/city/components/CityChannelCard.tsx`

**Interfaces:**
- Consumes: `cities: ActiveCityItem[]`；`cityChannelPath`
- Produces: 无「定位：上海」、无 `FILTER_ITEMS` Tab；保留标题 +「查看全部城市」+ 城市栅格

- [ ] **Step 1: 删除静态 UI**

删除：
1. `FilterItem` 类型与 `FILTER_ITEMS` 常量
2. Header 右侧「定位：上海」`Link`（保留「查看全部城市」）
3. 整段 `FILTER_ITEMS.map(...)` 横向 Tab 容器

清理因此未用的 import（如 `Flame`、`Target`、`ShieldCheck`、`CalendarDays`、`ChevronDown`；若 `MapPin` 仅用于定位按钮也可删）。

Header 右侧可简化为只保留「查看全部城市」按钮；若右侧变空则去掉 `flex` 右侧容器，标题区单独占满。

- [ ] **Step 2: 自检**

```bash
cd frontend && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-Object -First 40
```

Expected: 与 `CityChannelCard` 相关无报错。浏览器首页：无「定位：上海」、无假筛选 Tab，城市卡片仍可点。

- [ ] **Step 3: Commit（仅用户要求时）**

```bash
git add frontend/src/features/city/components/CityChannelCard.tsx
git commit -m "$(cat <<'EOF'
fix(frontend): remove static city channel location and filter tabs

EOF
)"
```

---

### Task 2: Flyway V161 + 留言接口权限（73）

**Files:**
- Create: `backend/taoke-app/src/main/resources/db/migration/V161__trainer_message_manage_permission.sql`
- Modify: `backend/taoke-admin/src/main/java/com/taoke/admin/controller/AdminTrainerMessageController.java`

**Interfaces:**
- Produces: permission `trainer-message:manage`；赋权 `PLATFORM_CS`、`PLATFORM_AUDITOR`
- Produces: 四个 admin 接口均 `@RequirePermission("trainer-message:manage")`

- [ ] **Step 1: 写 Flyway（对照 V144 权限种子写法）**

```sql
-- ============================================================
-- 专家留言管理权限（运营客服/审核员可处理）
-- ============================================================

INSERT INTO `sys_permissions` (`permission_code`, `permission_name`, `module`, `action_type`, `parent_id`, `sort_order`, `description`)
VALUES
    ('trainer-message:manage', '专家留言管理', 'interaction', 'EDIT', 0, 710, '平台运营-专家留言列表/处理/转需求');

INSERT INTO `sys_role_permissions` (`role_id`, `permission_id`, `created_at`)
SELECT
    (SELECT `id` FROM `sys_roles` WHERE `role_code` = 'PLATFORM_CS'),
    (SELECT `id` FROM `sys_permissions` WHERE `permission_code` = 'trainer-message:manage'),
    NOW()
WHERE EXISTS (SELECT 1 FROM `sys_roles` WHERE `role_code` = 'PLATFORM_CS')
  AND EXISTS (SELECT 1 FROM `sys_permissions` WHERE `permission_code` = 'trainer-message:manage')
  AND NOT EXISTS (
      SELECT 1 FROM `sys_role_permissions` rp
      WHERE rp.role_id = (SELECT `id` FROM `sys_roles` WHERE `role_code` = 'PLATFORM_CS')
        AND rp.permission_id = (SELECT `id` FROM `sys_permissions` WHERE `permission_code` = 'trainer-message:manage')
  );

INSERT INTO `sys_role_permissions` (`role_id`, `permission_id`, `created_at`)
SELECT
    (SELECT `id` FROM `sys_roles` WHERE `role_code` = 'PLATFORM_AUDITOR'),
    (SELECT `id` FROM `sys_permissions` WHERE `permission_code` = 'trainer-message:manage'),
    NOW()
WHERE EXISTS (SELECT 1 FROM `sys_roles` WHERE `role_code` = 'PLATFORM_AUDITOR')
  AND EXISTS (SELECT 1 FROM `sys_permissions` WHERE `permission_code` = 'trainer-message:manage')
  AND NOT EXISTS (
      SELECT 1 FROM `sys_role_permissions` rp
      WHERE rp.role_id = (SELECT `id` FROM `sys_roles` WHERE `role_code` = 'PLATFORM_AUDITOR')
        AND rp.permission_id = (SELECT `id` FROM `sys_permissions` WHERE `permission_code` = 'trainer-message:manage')
  );
```

- [ ] **Step 2: 校验 Flyway**

```bash
uv run python data-trans/scripts/_validate_flyway_migration.py --version 161
```

Expected: 通过。

- [ ] **Step 3: 改 Controller 鉴权**

`AdminTrainerMessageController.java`：

```java
import com.taoke.common.security.RequirePermission;
// 删除 BusinessRole / RequireRole 类级注解

@Tag(name = "后台-留言管理")
@RestController
@RequiredArgsConstructor
public class AdminTrainerMessageController {
    // ...

    @RequirePermission("trainer-message:manage")
    @GetMapping("/admin/trainer-messages")
    public ApiResponse<PageResponse<AdminTrainerMessageVO>> list(...) { ... }

    @RequirePermission("trainer-message:manage")
    @GetMapping("/admin/trainer-messages/{id}")
    public ApiResponse<AdminTrainerMessageVO> detail(...) { ... }

    @RequirePermission("trainer-message:manage")
    @PutMapping("/admin/trainer-messages/{id}/process")
    public ApiResponse<Void> markProcessed(...) { ... }

    @RequirePermission("trainer-message:manage")
    @PostMapping("/admin/trainer-messages/{id}/to-demand")
    public ApiResponse<DemandDetailResponse> convertToDemand(...) { ... }
}
```

路径变量若现有为 `{id}` 可保持；勿改业务调用。

- [ ] **Step 4: Commit（仅用户要求时）**

```bash
git add backend/taoke-app/src/main/resources/db/migration/V161__trainer_message_manage_permission.sql \
  backend/taoke-admin/src/main/java/com/taoke/admin/controller/AdminTrainerMessageController.java
git commit -m "$(cat <<'EOF'
feat(admin): allow ops to manage trainer lead messages

EOF
)"
```

---

### Task 3: 管理端导航与面包屑（73）

**Files:**
- Modify: `admin-frontend/src/config/nav-config.ts`
- Modify: `admin-frontend/src/hooks/use-breadcrumbs.tsx`

**Interfaces:**
- Produces: 侧栏「专家留言」→ `/dashboard/trainer-messages`

- [ ] **Step 1: nav-config 增加菜单项**

在「平台运营管理」内、`内训课报名` 项后插入：

```ts
{
  title: '专家留言',
  url: '/dashboard/trainer-messages',
  icon: 'forms',
  isActive: false,
  items: []
}
```

- [ ] **Step 2: breadcrumbs**

对照 `internal-course-enrollments`：

```ts
'trainer-messages': '专家留言',
```

以及：

```ts
'/dashboard/trainer-messages': [
  { title: 'Dashboard', link: '/dashboard' },
  { title: '平台运营管理', link: '/dashboard' },
  { title: '专家留言', link: '/dashboard/trainer-messages' }
],
```

若详情页路径 `/dashboard/trainer-messages/[id]` 需要独立 breadcrumb，按现有 open-course-enrollments 详情写法补一条（有则抄，无则列表级即可）。

- [ ] **Step 3: 自检**

```bash
cd admin-frontend && bun run build 2>&1 | Select-Object -Last 30
```

或至少确认 `nav-config` / breadcrumbs 无 TS 错误。登录超管：侧栏可见「专家留言」并可进入列表。

- [ ] **Step 4: Commit（仅用户要求时）**

```bash
git add admin-frontend/src/config/nav-config.ts admin-frontend/src/hooks/use-breadcrumbs.tsx
git commit -m "$(cat <<'EOF'
feat(admin-frontend): add trainer messages nav entry

EOF
)"
```

---

### Task 4: 公开课筛选 URL 读写齐全（76-B）

**Files:**
- Modify: `frontend/src/features/course/components/open/open-course-list-url.ts`
- Modify: `frontend/src/features/course/components/open/OpenCourseListSection.tsx`（`syncUrl`）

**Interfaces:**
- Produces: parse/sync 支持 `timeQuick`、`startTimeFrom`、`startTimeTo`、`priceMin`、`priceMax`、`isFree`、可选 `priceLabel`/`timeQuickLabel`

- [ ] **Step 1: 扩展 `parseOpenCourseFiltersFromSearch`**

在现有 category/province/city/institution/page 解析后追加：

```ts
const timeQuick = params.get('timeQuick') || undefined;
const timeQuickLabel = params.get('timeQuickLabel') || undefined;
const startTimeFrom = params.get('startTimeFrom') || undefined;
const startTimeTo = params.get('startTimeTo') || undefined;
const priceLabel = params.get('priceLabel') || undefined;
const priceMinRaw = params.get('priceMin');
const priceMaxRaw = params.get('priceMax');
const isFreeRaw = params.get('isFree');
const priceMin = priceMinRaw != null && priceMinRaw !== '' ? Number(priceMinRaw) : undefined;
const priceMax = priceMaxRaw != null && priceMaxRaw !== '' ? Number(priceMaxRaw) : undefined;
const isFree = isFreeRaw != null && isFreeRaw !== '' ? Number(isFreeRaw) : undefined;

// filters 合并：
filters: {
  // ...existing
  timeQuick,
  timeQuickLabel: timeQuickLabel ?? undefined,
  startTimeFrom,
  startTimeTo,
  priceLabel,
  priceMin: Number.isFinite(priceMin as number) ? priceMin : undefined,
  priceMax: Number.isFinite(priceMax as number) ? priceMax : undefined,
  isFree: Number.isFinite(isFree as number) ? isFree : undefined,
},
```

同步更新 `hasOpenCourseBrowserFilter` / `openCourseBrowserDiffersFromSsr`，把时间与价格纳入「有筛选 / 与 SSR 不同」判断。

预设反推（可选但推荐）：若有 `timeQuick` 无 label，按 `thisWeek→本周内` / `thisMonth→本月内` / `nextThreeMonths→近三个月` 填 `timeQuickLabel`。

- [ ] **Step 2: 扩展 `syncUrl`**

在 `OpenCourseListSection.syncUrl` 写入分类/省市之后追加：

```ts
if (f.timeQuick) params.set('timeQuick', f.timeQuick);
if (f.timeQuickLabel) params.set('timeQuickLabel', f.timeQuickLabel);
if (f.startTimeFrom) params.set('startTimeFrom', f.startTimeFrom);
if (f.startTimeTo) params.set('startTimeTo', f.startTimeTo);
if (f.priceLabel) params.set('priceLabel', f.priceLabel);
if (f.priceMin !== undefined && f.priceMin !== null) params.set('priceMin', String(f.priceMin));
if (f.priceMax !== undefined && f.priceMax !== null) params.set('priceMax', String(f.priceMax));
if (f.isFree !== undefined && f.isFree !== null) params.set('isFree', String(f.isFree));
```

- [ ] **Step 3: 自检**

本地打开 `/opencourse`，选「本月内」+ 某价格档，确认地址栏出现 `timeQuick`/`priceMin` 等；刷新后筛选与芯片仍在。

- [ ] **Step 4: Commit（仅用户要求时）**

```bash
git add frontend/src/features/course/components/open/open-course-list-url.ts \
  frontend/src/features/course/components/open/OpenCourseListSection.tsx
git commit -m "$(cat <<'EOF'
fix(frontend): persist open-course time and price filters in URL

EOF
)"
```

---

### Task 5: 芯片缺口 + SSR 初始筛选（76-A）

**Files:**
- Modify: `frontend/src/features/course/components/open/OpenCourseListSection.tsx`（`activeChips`）
- Modify: `frontend/src/app/[locale]/(public)/opencourses/page.tsx`
- Modify: `frontend/src/app/[locale]/(public)/city/[city]/opencourse/page.tsx`（若适用）

**Interfaces:**
- Consumes: Task 4 的 URL 字段
- Produces: 时间/价格无 label 也能出芯片；SSR `getCourseList` 带时间价格参数

- [ ] **Step 1: 修 chips 缺口**

将：

```ts
if (filters.timeQuick && filters.timeQuickLabel) {
```

改为有 `timeQuick` 即出芯片；label 用 `timeQuickLabel` 或 key 映射默认文案。

确认价格 chip：`isFree===1` 时文案为「免费」（若当前仅数字区间则补）。

- [ ] **Step 2: SSR page 传入初始筛选并请求**

`opencourses/page.tsx` 从 `searchParams` 读取 `timeQuick` / `startTimeFrom` / `startTimeTo` / `priceMin` / `priceMax` / `isFree`，传入：

1. `getCourseList({ ..., timeQuick, startTimeFrom, startTimeTo, priceMin, priceMax, isFree })`
2. `OpenCourseListSection` 的 `initial*` props（若组件尚无这些 props，在 Section 上增加并用于 `useState` 初始 `filters`）

城市频道公开课页同样带上（城市锁定已有；时间价格若 URL 有则一并）。

- [ ] **Step 3: 自检**

带 query 直开：`/opencourse?timeQuick=thisMonth&isFree=1` → 列表按筛选请求，芯片显示「开课时间」「价格：免费」类文案。

- [ ] **Step 4: Commit（仅用户要求时）**

```bash
git add frontend/src/features/course/components/open/OpenCourseListSection.tsx \
  frontend/src/app/[locale]/\(public\)/opencourses/page.tsx \
  frontend/src/app/[locale]/\(public\)/city/[city]/opencourse/page.tsx
git commit -m "$(cat <<'EOF'
fix(frontend): align open-course filter chips with SSR query state

EOF
)"
```

---

### Task 6: PXB 公开课已选条件汇总行（76-C）

**Files:**
- Modify: `frontend/src/features/course/components/open/pxb/PxbCourseListSection.tsx`

**Interfaces:**
- Consumes: 现有 `PxbCourseListUrlState` / `replacePxbCourseListUrl`
- Produces: 列表上方「已选条件」行（标签 + 叉 + 重置）

- [ ] **Step 1: 从 state 推导 active chips**

在排序栏与列表之间（或排序栏上方，对齐主站可读性）增加汇总行。chip 来源示例：

| state 字段 | 标签 |
|------------|------|
| keyword | 关键词：… |
| categoryId / subCategoryId | 分类：…（有名称用名称） |
| provinceId / cityId | 省市：… |
| timeQuick / 日期 | 开课时间：… |
| pricePreset / priceMin/Max | 价格：… |
| minScore | 评分：… |
| enrollStatus | 报名状态：… |

每项叉掉时写回 state 对应字段为清空并 `replacePxbCourseListUrl` + 重新拉列表。「重置」恢复 `DEFAULT_PXB_COURSE_LIST_STATE`（保留 embed `origin` 等必要参数，按现有 replace 工具处理）。

侧栏内现有 `FilterChip` 点选控件保留，不删除。

- [ ] **Step 2: 自检**

打开公开课 PXB embed URL，选分类/时间后汇总行出现；点叉后 URL 与列表同步更新。

- [ ] **Step 3: Commit（仅用户要求时）**

```bash
git add frontend/src/features/course/components/open/pxb/PxbCourseListSection.tsx
git commit -m "$(cat <<'EOF'
feat(frontend): show selected filter summary on PXB open course list

EOF
)"
```

---

### Task 7: 内训课程编号 = 老站 ID（78）

**Files:**
- Modify: `backend/taoke-course/src/main/java/com/taoke/course/service/CourseServiceImpl.java`
- Modify: `frontend/src/features/course/components/detail/CourseHero.tsx`

**Interfaces:**
- Produces: 非 open 课程 `displayCourseNo == course.id`；Hero 内训读 `displayCourseNo`

- [ ] **Step 1: 后端 assembleDetail**

将：

```java
vo.setDisplayCourseNo(resolveDisplayCourseNo(course.getId(), planEntities));
```

改为：

```java
if (course.getType() != null && course.getType().isOpen()) {
    vo.setDisplayCourseNo(resolveDisplayCourseNo(course.getId(), planEntities));
} else {
    // 内训：编号即老站 tk_courseinfo.id（迁移写入 courses.id）
    vo.setDisplayCourseNo(course.getId());
}
```

- [ ] **Step 2: 前端 CourseHero**

```tsx
const displayNo = isOpen
  ? resolveOpenCourseDisplayNo(course, pathId)
  : (course.displayCourseNo ?? course.id);
```

- [ ] **Step 3: 自检**

抽一门内训详情：API JSON 中 `displayCourseNo === id`；页面「课程编号」与该数字一致，并与老站同课 ID 对拍。

- [ ] **Step 4: Commit（仅用户要求时）**

```bash
git add backend/taoke-course/src/main/java/com/taoke/course/service/CourseServiceImpl.java \
  frontend/src/features/course/components/detail/CourseHero.tsx
git commit -m "$(cat <<'EOF'
fix(course): show legacy course id as internal course number

EOF
)"
```

---

### Task 8: 终验清单

- [ ] **Step 1: 对照 spec 勾选**

| Spec | 验收 |
|------|------|
| 77 | 首页无定位上海、无假 Tab，栅格正常 |
| 73 | V161 校验通过；侧栏有专家留言；CS/审核员权限码存在；接口非超管可访问（有权限时） |
| 76 | 时间/价格进 URL；刷新保留；芯片齐全；PXB 有已选行 |
| 78 | 内训编号 = `courses.id` = 老站 ID |

- [ ] **Step 2: 工作区注意**

先前 SSR `skipAuth` 空列表修复若不在本计划文件内，终验时确认是否单独保留/提交，勿被本批覆盖回退。

---

## Spec coverage（自检）

| Spec 节 | Task |
|---------|------|
| §77 | Task 1 |
| §73 权限+Controller | Task 2 |
| §73 导航 | Task 3 |
| §76 URL | Task 4 |
| §76 芯片+SSR | Task 5 |
| §76 PXB | Task 6 |
| §78 | Task 7 |
| 终验 | Task 8 |

无 TBD/占位步骤；Flyway 版本固定 V161。
